#include <stdio.h>

int table[10000];

void main(){
	int N, M;

	for(int i=0; i<20; i++){
		scanf("%d%d", &N, &M);
		if(N==0&&M==0){
			break;
		}
		for(int j=0; j<N; j++){
			scanf("%d", &table[j]);
			count
		}
		free(counttable);
	}
	return 0;
}
